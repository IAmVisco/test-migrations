{
  "name": "main",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "sequelize-cli db:drop && sequelize-cli db:create && sequelize-cli db:migrate && node test.js && sequelize-cli db:migrate:undo && sequelize-cli db:drop"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "pg": "^8.7.1",
    "pg-hstore": "^2.3.4",
    "sequelize": "^6.6.5",
    "sequelize-cli": "^6.2.0"
  },
  "dependencies": {
    "child": "file:../child",
    "faker": "^5.5.3"
  }
}
